<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pc.autopetfeeder">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.SET_ALARM" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_big"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_big"
        android:logo="@drawable/ccat"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".intro">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".cat" />
        <activity android:name=".dog" />
        <activity android:name=".now_eat" />
        <activity android:name=".res_eat" />
        <receiver android:name=".BroadcastD" />

        <activity android:name=".MainActivity"></activity>
    </application>

</manifest>